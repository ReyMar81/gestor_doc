# --- Configuración Frontend para Producción ---

# URL de la API del backend
# Usar ruta relativa si frontend y backend están en el mismo servidor
# o especificar la URL completa del backend
VITE_API_URL=/api

# URL del WebSocket
# Usar ruta relativa para que se adapte automáticamente al protocolo (ws/wss)
# o especificar la URL completa
# VITE_WS_URL=ws://tu-ip-o-dominio
# VITE_WS_URL=wss://tu-dominio-con-ssl
# Si no se especifica, se usará automáticamente el host actual
